# inventory.ts

Purpose: register `/waldemar-inventory`.

Reports:

- configured pi packages from `~/.pi/agent/settings.json`
- configured MCP servers from `~/.pi/agent/mcp.json`
- installed global skills from `~/.agents/skills` and `~/.pi/agent/skills`
